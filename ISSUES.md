# JWTVL Issue Tracker

Generated: 2026-07-20. Updated on each work session.

---

## Legend

| Symbol | Meaning        |
|--------|----------------|
| ✅     | Done           |
| 🔄     | In progress    |
| ⬜     | Pending        |
| ❌     | Won't fix / dropped |

**Effort:** XS < 1h · S < 4h · M < 1d · L < 3d

---

## P0 — Must Fix (Actively Broken) — ✅ ALL DONE (v3.0.9 / code 12)

| ID     | Status | Effort | Impact | Description |
|--------|--------|--------|--------|-------------|
| BUG-1  | ✅ | S | HIGH | Fixed scroll-to-top: `rememberLazyListState()` passed to `LazyColumn`; `LaunchedEffect(categoryKey, uiState)` calls `animateScrollToItem(0)` on every category switch and Success state. Sidebar nav buttons also scroll explicitly on click. |
| BUG-2  | ✅ | S | HIGH | Sidebar width fixed at `240.dp` in layout tree — no layout thrash. `graphicsLayer { translationX = offset.dp.toPx() }` slides it −170dp off-screen when collapsed, leaving 70dp icon rail. Zero recomposition impact on main content. |
| BUG-3  | ✅ | S | MED  | `updateDialogDismissed: Boolean` hoisted to ViewModel. "Later" calls `dismissUpdateDialog()`. `checkForUpdates` resets it to `false` on new version. Eliminates the dead `remember { mutableStateOf(true) }` that permanently suppressed the dialog. |
| BUG-4  | ✅ | XS | MED  | `isMinifyEnabled = true` + `proguard-rules.pro` covering serialization, OkHttp, ExoPlayer, Compose. APK dropped from 10.0 MB → 5.9 MB. |
| BUG-6  | ✅ | S | HIGH | `VideoCard` uses `androidx.tv.material3.Surface(onClick)`. D-pad center/enter fires correctly. Focus highlight via `ClickableSurfaceDefaults.border(focusedBorder = …)`. |
| PERF-3 | ✅ | S | HIGH | `CacheManager.getJson`/`saveJson` are `suspend` dispatched on `Dispatchers.IO`. `getCachedCategory`/`getCachedLanguages` in `MediatorClient` are `suspend`. `loadCachedSubcategories` in ViewModel is `suspend`. No blocking IO on main thread. |

---

## P1 — Should Fix (Performance / UX Quality) — ✅ ALL DONE (v3.1.0 / code 13)

| ID     | Status | Effort | Impact | Description |
|--------|--------|--------|--------|-------------|
| PERF-1 | ✅ | L | HIGH | Migrated to `TvLazyColumn` + `TvLazyRow` from `androidx.tv.foundation` — TV-native scroll physics and focus handling. `rememberTvLazyListState()` for programmatic scroll control. |
| PERF-2 | ✅ | S | MED  | `JwTvApp : Application, ImageLoaderFactory` provides a Coil `ImageLoader` with 25% heap memory cache (≈64 MB) and 256 MB disk cache. Hero `AsyncImage` uses explicit `size(1280, 720)` decode hint. `crossfade(false)` removes per-frame delay. Registered in `AndroidManifest.xml`. |
| PERF-4 | ✅ | S | LOW  | All sidebar emoji `Text("🏠")` replaced with `Icons.Filled.*` `ImageVector` drawables rendered via `androidx.compose.material3.Icon`. GPU vector paths, no Unicode shaping pipeline. `material-icons-extended:1.6.8` added to dependencies. |
| PERF-5 | ✅ | M | HIGH | `@Immutable` added to all API data classes (`CategoryData`, `MediaItem`, `ImageDetail`, `MediaFile`, `SubtitleInfo`, `LanguageInfo`, `CategoryResponse`, `LanguageResponse`) and `SubcategoryWithMedia`. Compose compiler can now skip recomposition when the same stable reference is passed. |
| PERF-6 | ❌ | — | —    | Covered by BUG-2 (GPU translate replaced layout-width animation entirely). |
| BUG-5  | ✅ | M | MED  | Subcategory fetch uses `Mutex`-guarded incremental emit: each resolved subcategory appends to `subcategoriesWithMedia` and sets `uiState = Success` immediately, so rows appear one by one as they load rather than all at once. Final pass re-orders to match server ordering. Parallelism capped at 3 concurrent requests via `Dispatchers.IO.limitedParallelism(3)`. |
| UX-3   | ✅ | M | MED  | `SubcomposeAsyncImage` with `loading = { ShimmerBox() }` in `VideoCard` and `HeroBanner`. `ShimmerBox` is an `infiniteRepeatable` animated `linearGradient` sweeping across the card area. |
| UX-5   | ✅ | S | HIGH | ViewModel stores `featuredVideo: MediaItem?` populated from `root.media?.firstOrNull()` (the editorially curated JW mediator field) on every `VideoOnDemand` response. `VideoBrowserScreen` uses this first, falling back to `subcategoriesWithMedia.first().videos.first()`. |
| UX-6   | ✅ | S | HIGH | Main content `Box` has `onKeyEvent { if (Left) sidebarFirstFocus.requestFocus() }`. `sidebarFirstFocus: FocusRequester` is attached to the Home sidebar item. D-pad Left from any content row now opens the sidebar drawer. |
| UX-8   | ✅ | XS | LOW | Version string in sidebar reads `BuildConfig.VERSION_NAME` (generated by Gradle). `buildConfig = true` added to `buildFeatures {}` in `app/build.gradle.kts`. No more hardcoded `"v3.0.8"`. |

---

## P2 — Nice to Have (Features / Architecture) — ⬜ PENDING

| ID     | Status | Effort | Impact | Description |
|--------|--------|--------|--------|-------------|
| ARCH-1 | ⬜ | L | HIGH | Proper ViewModel state encapsulation: all fields `private set`; expose `StateFlow<ScreenState>`; UI writes via action methods only |
| ARCH-2 | ⬜ | L | HIGH | Adopt `navigation-compose` with TV-aware route graph: `Home`, `Player/{videoId}`, `Search`, `Settings` |
| ARCH-3 | ⬜ | M | MED  | Move `OkHttpClient` init to `Application.onCreate` via manual DI; constructor-inject into ViewModel via `ViewModelProvider.Factory` |
| ARCH-4 | ⬜ | M | MED  | TTL-based cache expiry in `CacheManager`: timestamp alongside JSON; categories TTL 24h, video lists 6h; stale-while-revalidate up to 7 days |
| ARCH-5 | ⬜ | L | HIGH | Search screen using JW mediator `/search/{lang}?q=` endpoint |
| ARCH-6 | ⬜ | L | HIGH | Favorites / Watchlist with Room DB persistence |
| UX-2   | ⬜ | M | MED  | Replace `VideoDetailsDialog` Material3 `AlertDialog` with TV-native full-width overlay `Surface` with explicit `FocusRequester` on primary action |
| UX-4   | ⬜ | M | HIGH | "Continue Watching" row on home screen — progress already saved in SharedPreferences, needs UI consumer |
| UX-7   | ⬜ | S | MED  | Seek feedback toast in player: "+10s" / "-10s" overlay on D-pad left/right press |
| BUG-7  | ⬜ | S | LOW  | Stable watch-progress key: use URL hash instead of `video.title` (not unique across series episodes) |

---

## Changelog

| Date       | Version     | Changes |
|------------|-------------|---------|
| 2026-07-20 | 3.0.8 / 11  | Initial audit. Plan established. |
| 2026-07-20 | 3.0.9 / 12  | **P0 complete**: BUG-1 scroll-to-top, BUG-2 sidebar GPU slide, BUG-3 dialog state hoisted, BUG-4 R8 enabled (10 MB→5.9 MB), BUG-6 TV Surface on cards, PERF-3 cache IO off main thread. |
| 2026-07-20 | 3.1.0 / 13  | **P1 complete**: PERF-1 TvLazyColumn/Row, PERF-2 Coil 64 MB cache + disk cache, PERF-4 Material Icons vector (no emoji), PERF-5 @Immutable on all data classes, BUG-5 incremental subcategory emit, UX-3 shimmer placeholders, UX-5 editorial featured video from API, UX-6 D-pad Left opens sidebar, UX-8 BuildConfig.VERSION_NAME. APK: 9.1 MB (larger due to Material Icons Extended). |
